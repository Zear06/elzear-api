const collections = ['users', 'groups', 'auth_local', 'auth_facebook'];
const edgeCollections = ['groups_users', 'users_auths', 'comments', 'polls', 'preferences'];

export {
  collections,
  edgeCollections
};
